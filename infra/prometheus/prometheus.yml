global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "esp32-env-monitor"
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/*.json
        refresh_interval: 30s
